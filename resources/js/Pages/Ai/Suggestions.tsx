import React from 'react';
// TODO: pending AI suggestions queue -- Accept/Reject buttons call
// ai.suggestions.accept / ai.suggestions.reject. Use the existing
// AlertDialog component (not confirm()) for the reject confirmation.
export default function Suggestions() {
  return <div className="p-6">AI suggestions review queue.</div>;
}

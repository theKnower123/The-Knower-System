import React from 'react';
// TODO: drag-and-drop section reorder (e.g. dnd-kit) + portfolio visibility toggles.
// Each toggle calls POST cms.landing.portfolio.toggle — backend enforces client_approved.
export default function LandingBuilder() {
  return <div className="p-6">Landing page builder — sections + portfolio visibility toggles.</div>;
}

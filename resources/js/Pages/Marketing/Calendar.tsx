import React from 'react';
// TODO: Content calendar view (list or calendar grid) of posts.
// Mirrors the pattern used in ProjectsIndex.tsx for list rendering + status badges.
export default function Calendar() {
  return <div className="p-6">Marketing content calendar — build on top of ProjectsIndex.tsx pattern.</div>;
}
